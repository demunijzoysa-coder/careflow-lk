{
  "name": "careflow-lk",
  "private": true,
  "version": "0.1.0",
  "description": "Offline-first care continuity system for rural clinics in Sri Lanka — patient timelines, follow-ups, SMS reminders, and medication adherence tracking (no diagnosis).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "format": "prettier . --write",
    "lint": "eslint .",
    "dev": "npm -w apps/clinic-pwa run dev",
    "dev:api": "npm -w apps/api run dev",
    "start": "npm run dev",
    "stop": "node scripts/stop.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
